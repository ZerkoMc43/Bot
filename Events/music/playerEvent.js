const { EmbedBuilder } = require("discord.js");

module.exports = {
    name: "playerEvent",
    async run(error){
        print(error)
        try {
            
        } catch(err){
            console.error('Erreur avec discord-player:', error);
        }
    }
}